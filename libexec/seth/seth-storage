#!/usr/bin/env bash
### seth-storage -- show the value of a storage slot on the blockchain
### Usage: seth storage [-b <block>] <address> <slot>
### Show the value of a storage slot on the blockchain.
### If `-b' is given, print the value as of <block>.
exec seth --jsonrpc eth_getStorageAt -- \
-s "${1?missing address}" -iappend \
-s "${2?missing position}" -iappend \
${SETH_BLOCK+-s "${SETH_BLOCK?}" -iappend}
