#!/bin/sh
### seth-code -- print the bytecode of a contract on the blockchain
### Usage: seth code [-b <block>] <contract>
### Print the bytecode of <contract> as recorded on the blockchain.
###
### If `-b' is given, print the bytecode as of <block>.

exec seth rpc2 eth_getCode -- \
  -s "${1?}"         -iappend \
  -s "${ETH_BLOCK?}" -iappend
