#!/bin/sh
### seth-count -- print the number of transactions sent from an address
### Usage: seth count [-b <block>] <address>
### Print the number of transactions sent from <address>.
###
### If `-b' is given, print the count as of <block>.

exec seth rpc eth_getTransactionCount -- \
  -s "${1?}"         -iappend            \
  -s "${ETH_BLOCK?}" -iappend
