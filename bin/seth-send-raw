#!/bin/sh
### seth-send-raw -- publish a previously signed transaction
### Usage: seth send-raw
### Publish a previously signed transaction to the blockchain.
### The raw transaction data is read from standard input.

exec seth rpc2 eth_sendRawTransaction -- \
  -s "`cat`" -iappend
