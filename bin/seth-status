#!/bin/sh
### seth-status -- display client and network status information

for property in block gas-price mining network version; do
  printf "%-15s %s\n" "$property" "$(seth $property || echo -)" &
done | LANG=C sort; wait
