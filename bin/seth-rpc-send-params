#!/usr/bin/env bash
items=(
  from:SETH_ACCOUNT
  gas:SETH_GAS
  gasPrice:SETH_GAS_PRICE_WEI
  value:SETH_MONEY_WEI
)
for item in ${items[*]}; do
  property=${item%:*} variable=${item#*:}
  [[ ${!variable} ]] && echo -s ${!variable} -i $property
done
