#!/bin/sh
cat <<EOF
Usage: seth <command> [<options>]
   or: seth --rpc <method> [<params>]

Commands:

$(grep -hoP "^(\\S)\\1\\1 seth-\\K[^ ]* -- .*" \
  $(dirname $0)/seth-* | sed "s/ -- / /" |
  while read name description; do
    printf "   %-12s  %s\n" "$name" "$description"
  done)

EOF
