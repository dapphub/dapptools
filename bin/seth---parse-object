#!/bin/sh
object=`cat`
if keys=$(echo "$object" | jshon -Q -k); then
  for key in $keys; do
    value=$(echo "$object" | jshon -e "$key" -u)
    printf "%s\t%s\n" "$key" "$value"
  done
else
  echo "$object"
fi
