#!/usr/bin/env bash
if [[ $1 ]]; then
  hexdata=$1
else
  hexdata=`cat`
fi

hexnum=${hexdata#0x}
if [[ $hexnum =~ ^[[:xdigit:]]+ ]]; then
  echo 0x${hexnum,,}
else  
  echo >&2 "${0##*/}: error: invalid hexdata: $hexdata"
  exit 1
fi
